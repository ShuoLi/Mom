<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :remindTime %><br>
    <%= f.datetime_select :remindTime %>
  </div>
  <div class="field">
    <%= f.label :repeat %><br>
    <%= f.text_field :repeat %>
  </div>
  <div class="field">
    <%= f.label :lastComplete %><br>
    <%= f.datetime_select :lastComplete %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
